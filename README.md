# Gacha Tracker 0.5.6

## 本版修正
- 不再按创建时间强制倒序
- 编辑/新建卡池时新增“显示顺序”
- 数字越大越靠上，数字越小越靠下
- 例如：3 显示在最上面，2 在中间，1 在最下面
- 新建卡池默认使用当前最大序号 + 1，因此默认显示在最上面；可以手动修改
- 旧卡池会自动获得 1、2、3…… 的顺序号，并且之后可随时编辑调整

同时保留 0.5.5 的角色图片完整显示、“绝密”名称、去掉“其他”卡池、点击后显示编辑/删除等功能。
